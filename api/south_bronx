-- South Bronx Script with GUI - Ragdoll Fly + ESP + Key System
-- Made by: sadx8992

local GITHUB_RAW = "https://raw.githubusercontent.com/sadx8992-lgtm/jean14_17/main"
local KEYS_URL = GITHUB_RAW .. "/keys.json"

-- Variables
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

local FlyActive = false
local FlySpeed = 50
local ESPActive = false
local ScriptActive = false
local RagdollParts = {}
local ESPPlayers = {}
local GuiActive = true

-- =====================
-- CREAR GUI
-- =====================

local function CreateGui()
    -- Crear ScreenGui principal
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "SouthBronxGui"
    screenGui.ResetOnSpawn = false
    screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
    
    -- Panel principal
    local mainPanel = Instance.new("Frame")
    mainPanel.Name = "MainPanel"
    mainPanel.Size = UDim2.new(0, 400, 0, 500)
    mainPanel.Position = UDim2.new(0.5, -200, 0.5, -250)
    mainPanel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    mainPanel.BorderColor3 = Color3.fromRGB(0, 255, 0)
    mainPanel.BorderSizePixel = 3
    mainPanel.Parent = screenGui
    
    -- Título
    local title = Instance.new("TextLabel")
    title.Name = "Title"
    title.Size = UDim2.new(1, 0, 0, 40)
    title.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    title.TextColor3 = Color3.fromRGB(0, 0, 0)
    title.TextSize = 24
    title.Font = Enum.Font.GothamBold
    title.Text = "SOUTH BRONX"
    title.Parent = mainPanel
    
    -- Status Label
    local statusLabel = Instance.new("TextLabel")
    statusLabel.Name = "StatusLabel"
    statusLabel.Size = UDim2.new(1, -20, 0, 30)
    statusLabel.Position = UDim2.new(0, 10, 0, 50)
    statusLabel.BackgroundTransparency = 1
    statusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
    statusLabel.TextSize = 16
    statusLabel.Font = Enum.Font.Gotham
    statusLabel.Text = "Status: [NO VALIDADO]"
    statusLabel.TextXAlignment = Enum.TextXAlignment.Left
    statusLabel.Parent = mainPanel
    
    -- Input Key Label
    local keyLabel = Instance.new("TextLabel")
    keyLabel.Name = "KeyLabel"
    keyLabel.Size = UDim2.new(1, -20, 0, 20)
    keyLabel.Position = UDim2.new(0, 10, 0, 90)
    keyLabel.BackgroundTransparency = 1
    keyLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    keyLabel.TextSize = 14
    keyLabel.Font = Enum.Font.Gotham
    keyLabel.Text = "Ingresa o pega tu KEY:"
    keyLabel.TextXAlignment = Enum.TextXAlignment.Left
    keyLabel.Parent = mainPanel
    
    -- TextBox para pegar KEY
    local keyInput = Instance.new("TextBox")
    keyInput.Name = "KeyInput"
    keyInput.Size = UDim2.new(1, -20, 0, 35)
    keyInput.Position = UDim2.new(0, 10, 0, 115)
    keyInput.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    keyInput.BorderColor3 = Color3.fromRGB(0, 255, 0)
    keyInput.BorderSizePixel = 2
    keyInput.TextColor3 = Color3.fromRGB(0, 255, 0)
    keyInput.TextSize = 14
    keyInput.Font = Enum.Font.Gotham
    keyInput.PlaceholderText = "Ctrl+V para pegar | KEY-SB2024-XXXXXX"
    keyInput.PlaceholderColor3 = Color3.fromRGB(100, 100, 100)
    keyInput.ClearTextOnFocus = false
    keyInput.Parent = mainPanel
    
    -- Botón Validar
    local validateBtn = Instance.new("TextButton")
    validateBtn.Name = "ValidateBtn"
    validateBtn.Size = UDim2.new(1, -20, 0, 40)
    validateBtn.Position = UDim2.new(0, 10, 0, 160)
    validateBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
    validateBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
    validateBtn.TextSize = 16
    validateBtn.Font = Enum.Font.GothamBold
    validateBtn.Text = "✓ VALIDAR KEY"
    validateBtn.Parent = mainPanel
    
    -- Separador 1
    local separator1 = Instance.new("Frame")
    separator1.Name = "Separator1"
    separator1.Size = UDim2.new(1, -20, 0, 2)
    separator1.Position = UDim2.new(0, 10, 0, 210)
    separator1.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
    separator1.BorderSizePixel = 0
    separator1.Parent = mainPanel
    
    -- Label de Features
    local featuresLabel = Instance.new("TextLabel")
    featuresLabel.Name = "FeaturesLabel"
    featuresLabel.Size = UDim2.new(1, -20, 0, 20)
    featuresLabel.Position = UDim2.new(0, 10, 0, 225)
    featuresLabel.BackgroundTransparency = 1
    featuresLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    featuresLabel.TextSize = 14
    featuresLabel.Font = Enum.Font.GothamBold
    featuresLabel.Text = "FEATURES:"
    featuresLabel.TextXAlignment = Enum.TextXAlignment.Left
    featuresLabel.Parent = mainPanel
    
    -- Botón Fly
    local flyBtn = Instance.new("TextButton")
    flyBtn.Name = "FlyBtn"
    flyBtn.Size = UDim2.new(0.5, -7, 0, 45)
    flyBtn.Position = UDim2.new(0, 10, 0, 255)
    flyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    flyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    flyBtn.TextSize = 14
    flyBtn.Font = Enum.Font.GothamBold
    flyBtn.Text = "✈️ FLY\n[OFF]"
    flyBtn.Parent = mainPanel
    
    -- Botón ESP
    local espBtn = Instance.new("TextButton")
    espBtn.Name = "EspBtn"
    espBtn.Size = UDim2.new(0.5, -7, 0, 45)
    espBtn.Position = UDim2.new(0.5, 4, 0, 255)
    espBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    espBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    espBtn.TextSize = 14
    espBtn.Font = Enum.Font.GothamBold
    espBtn.Text = "👁️ ESP\n[OFF]"
    espBtn.Parent = mainPanel
    
    -- Info Text
    local infoText = Instance.new("TextLabel")
    infoText.Name = "InfoText"
    infoText.Size = UDim2.new(1, -20, 0, 100)
    infoText.Position = UDim2.new(0, 10, 0, 315)
    infoText.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    infoText.BorderColor3 = Color3.fromRGB(0, 255, 0)
    infoText.BorderSizePixel = 1
    infoText.TextColor3 = Color3.fromRGB(0, 255, 0)
    infoText.TextSize = 12
    infoText.Font = Enum.Font.Gotham
    infoText.TextWrapped = true
    infoText.TextXAlignment = Enum.TextXAlignment.Left
    infoText.TextYAlignment = Enum.TextYAlignment.Top
    infoText.Text = "📋 CONTROLES:\n\n• F: Toggle Fly (Ragdoll)\n• E: Toggle ESP\n• F9: Ver Consola\n\n⚠️ Valida tu KEY primero!"
    infoText.Parent = mainPanel
    
    -- Botón Cerrar
    local closeBtn = Instance.new("TextButton")
    closeBtn.Name = "CloseBtn"
    closeBtn.Size = UDim2.new(1, -20, 0, 30)
    closeBtn.Position = UDim2.new(0, 10, 0, 460)
    closeBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextSize = 14
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Text = "✕ CERRAR"
    closeBtn.Parent = mainPanel
    
    return {
        gui = screenGui,
        panel = mainPanel,
        statusLabel = statusLabel,
        keyInput = keyInput,
        validateBtn = validateBtn,
        flyBtn = flyBtn,
        espBtn = espBtn,
        closeBtn = closeBtn
    }
end

-- =====================
-- KEY VALIDATION SYSTEM
-- =====================

local function ValidateKey(key)
    local success, response = pcall(function()
        return game:HttpGet(KEYS_URL)
    end)
    
    if not success then
        print("[ERROR] No se pudo conectar al servidor de keys")
        return false
    end
    
    local keys_data = HttpService:JSONDecode(response)
    
    -- Verificar si la key está en available_keys
    for _, available_key in ipairs(keys_data.available_keys) do
        if available_key == key then
            -- Key válida, verificar si ya fue usada
            for _, used_key in ipairs(keys_data.used_keys) do
                if used_key == key then
                    print("[⚠️] Key Usada - Esta llave ya fue utilizada")
                    return false
                end
            end
            print("[✓] Key Válida - Acceso Concedido")
            ScriptActive = true
            return true
        end
    end
    
    print("[✗] Key Inválida")
    return false
end

-- =====================
-- RAGDOLL FLY SYSTEM
-- =====================

local function CreateRagdoll()
    local character = LocalPlayer.Character
    if not character then return end
    
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanCollide = false
        end
    end
    
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if rootPart then
        local bodyVelocity = Instance.new("BodyVelocity")
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        bodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bodyVelocity.Parent = rootPart
        RagdollParts.bodyVelocity = bodyVelocity
        
        local bodyGyro = Instance.new("BodyGyro")
        bodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        bodyGyro.Parent = rootPart
        RagdollParts.bodyGyro = bodyGyro
    end
    
    local humanoid = character:FindFirstChild("Humanoid")
    if humanoid then
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, false)
        humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, false)
        humanoid.PlatformStand = true
    end
end

local function StartFly()
    if not ScriptActive then
        print("[!] Debes validar una key primero")
        return
    end
    
    local character = LocalPlayer.Character
    if not character then return end
    
    FlyActive = true
    CreateRagdoll()
    
    local connection
    connection = game:GetService("RunService").RenderStepped:Connect(function()
        if not FlyActive or not LocalPlayer.Character then
            connection:Disconnect()
            if RagdollParts.bodyVelocity then
                RagdollParts.bodyVelocity:Destroy()
            end
            if RagdollParts.bodyGyro then
                RagdollParts.bodyGyro:Destroy()
            end
            local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
            if humanoid then
                humanoid.PlatformStand = false
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Landed, true)
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Running, true)
                humanoid:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, true)
            end
            RagdollParts = {}
            return
        end
        
        local moveDirection = Vector3.new(0, 0, 0)
        
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            moveDirection = moveDirection + (Camera.CFrame.LookVector * Vector3.new(1, 0, 1)).Unit
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            moveDirection = moveDirection - (Camera.CFrame.LookVector * Vector3.new(1, 0, 1)).Unit
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            moveDirection = moveDirection - Camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            moveDirection = moveDirection + Camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            moveDirection = moveDirection + Vector3.new(0, 1, 0)
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            moveDirection = moveDirection - Vector3.new(0, 1, 0)
        end
        
        if moveDirection.Magnitude > 0 then
            moveDirection = moveDirection.Unit
        end
        
        if RagdollParts.bodyVelocity then
            RagdollParts.bodyVelocity.Velocity = moveDirection * FlySpeed
        end
        if RagdollParts.bodyGyro then
            RagdollParts.bodyGyro.CFrame = Camera.CFrame
        end
    end)
    
    print("[✈️] Ragdoll Fly Activado")
end

local function StopFly()
    FlyActive = false
    print("[✈️] Fly Desactivado")
end

-- =====================
-- ESP SYSTEM
-- =====================

local function CreateESPGui(player)
    if player == LocalPlayer then return end
    if not player.Character then return end
    
    local character = player.Character
    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
    if not humanoidRootPart then return end
    
    local billboardGui = Instance.new("BillboardGui")
    billboardGui.Size = UDim2.new(4, 0, 2, 0)
    billboardGui.MaxDistance = 500
    billboardGui.Parent = humanoidRootPart
    
    local textLabel = Instance.new("TextLabel")
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    textLabel.BackgroundTransparency = 0.5
    textLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    textLabel.TextSize = 18
    textLabel.Font = Enum.Font.GothamBold
    textLabel.Text = player.Name
    textLabel.Parent = billboardGui
    
    local box = Instance.new("BoxHandleAdornment")
    box.Size = humanoidRootPart.Size + Vector3.new(0.5, 2, 0.5)
    box.Color3 = Color3.fromRGB(0, 255, 0)
    box.Transparency = 0.3
    box.Parent = humanoidRootPart
    
    return {gui = billboardGui, label = textLabel, box = box}
end

local function StartESP()
    if not ScriptActive then
        print("[!] Debes validar una key primero")
        return
    end
    
    ESPActive = true
    print("[👁️] ESP Activado")
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not ESPPlayers[player] then
            ESPPlayers[player] = CreateESPGui(player)
        end
    end
    
    local playerAddedConnection
    playerAddedConnection = Players.PlayerAdded:Connect(function(player)
        if ESPActive and player ~= LocalPlayer then
            wait(0.5)
            ESPPlayers[player] = CreateESPGui(player)
        end
    end)
    
    local espUpdateConnection
    espUpdateConnection = game:GetService("RunService").RenderStepped:Connect(function()
        if not ESPActive then
            espUpdateConnection:Disconnect()
            playerAddedConnection:Disconnect()
            return
        end
        
        for player, espData in pairs(ESPPlayers) do
            if player and player.Parent and player.Character then
                local character = player.Character
                local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                local humanoid = character:FindFirstChild("Humanoid")
                
                if humanoidRootPart and humanoid then
                    local distance = (LocalPlayer.Character.HumanoidRootPart.Position - humanoidRootPart.Position).Magnitude
                    local health = math.floor(humanoid.Health)
                    espData.label.Text = player.Name .. " [" .. math.floor(distance) .. "m] " .. health .. "HP"
                    
                    if health > 50 then
                        espData.label.TextColor3 = Color3.fromRGB(0, 255, 0)
                        espData.box.Color3 = Color3.fromRGB(0, 255, 0)
                    elseif health > 25 then
                        espData.label.TextColor3 = Color3.fromRGB(255, 255, 0)
                        espData.box.Color3 = Color3.fromRGB(255, 255, 0)
                    else
                        espData.label.TextColor3 = Color3.fromRGB(255, 0, 0)
                        espData.box.Color3 = Color3.fromRGB(255, 0, 0)
                    end
                else
                    pcall(function()
                        espData.gui:Destroy()
                        espData.box:Destroy()
                    end)
                    ESPPlayers[player] = nil
                end
            else
                pcall(function()
                    if espData then
                        espData.gui:Destroy()
                        espData.box:Destroy()
                    end
                end)
                ESPPlayers[player] = nil
            end
        end
    end)
    
    Players.PlayerRemoving:Connect(function(player)
        if ESPPlayers[player] then
            pcall(function()
                ESPPlayers[player].gui:Destroy()
                ESPPlayers[player].box:Destroy()
            end)
            ESPPlayers[player] = nil
        end
    end)
end

local function StopESP()
    ESPActive = false
    for player, espData in pairs(ESPPlayers) do
        pcall(function()
            espData.gui:Destroy()
            espData.box:Destroy()
        end)
    end
    ESPPlayers = {}
    print("[👁️] ESP Desactivado")
end

-- =====================
-- INIT GUI
-- =====================

print("\n========================================")
print("  South Bronx Script - GUI Edition v2")
print("========================================\n")

local gui = CreateGui()

-- Validar Key
gui.validateBtn.MouseButton1Click:Connect(function()
    local key = gui.keyInput.Text
    if key == "" then
        gui.statusLabel.Text = "Status: [❌ INGRESA UNA KEY]"
        gui.statusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
        return
    end
    
    if ValidateKey(key) then
        gui.statusLabel.Text = "Status: [✅ VALIDADO]"
        gui.statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
        gui.keyInput.Editable = false
        gui.validateBtn.Text = "✓ VALIDADO"
        gui.validateBtn.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    else
        gui.statusLabel.Text = "Status: [❌ KEY INVÁLIDA]"
        gui.statusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    end
end)

-- Botón Fly
gui.flyBtn.MouseButton1Click:Connect(function()
    if FlyActive then
        StopFly()
        gui.flyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        gui.flyBtn.Text = "✈️ FLY\n[OFF]"
    else
        StartFly()
        gui.flyBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        gui.flyBtn.Text = "✈️ FLY\n[ON]"
    end
end)

-- Botón ESP
gui.espBtn.MouseButton1Click:Connect(function()
    if ESPActive then
        StopESP()
        gui.espBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
        gui.espBtn.Text = "👁️ ESP\n[OFF]"
    else
        StartESP()
        gui.espBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
        gui.espBtn.Text = "👁️ ESP\n[ON]"
    end
end)

-- Botón Cerrar
gui.closeBtn.MouseButton1Click:Connect(function()
    gui.gui:Destroy()
    GuiActive = false
end)

-- Atajos de teclado
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == Enum.KeyCode.F then
        if FlyActive then
            StopFly()
            gui.flyBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
            gui.flyBtn.Text = "✈️ FLY\n[OFF]"
        else
            StartFly()
            gui.flyBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            gui.flyBtn.Text = "✈️ FLY\n[ON]"
        end
    elseif input.KeyCode == Enum.KeyCode.E then
        if ESPActive then
            StopESP()
            gui.espBtn.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
            gui.espBtn.Text = "👁️ ESP\n[OFF]"
        else
            StartESP()
            gui.espBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            gui.espBtn.Text = "👁️ ESP\n[ON]"
        end
    end
end)

_G.ValidateKey = ValidateKey
_G.StartFly = StartFly
_G.StopFly = StopFly
_G.StartESP = StartESP
_G.StopESP = StopESP

return true
